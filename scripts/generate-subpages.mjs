/**
 * Post-build script: generates SEO subpages from dist/index.html
 *
 * NOTE: /tatuagem-brasília subpage generation is DISABLED.
 * The board wants that URL to serve the OLD (pre-alterations) design,
 * which requires a separate frozen dist uploaded directly to Hostinger.
 * This script is kept as a placeholder for future subpages.
 *
 * Usage: node scripts/generate-subpages.mjs
 */

console.log('Subpage generation: no subpages configured (tatuagem-brasília frozen separately).');
